export enum PetGender {
  Male = 'MALE',
  Female = 'FEMALE',
  Other = 'Other',
}

export enum OrderDirection {
  Asc = 'ASC',
  Desc = 'DESC',
}

export enum PetOrderBy {
  Name = 'name',
  CreatedAt = 'createdAt',
  UpdatedAt = 'updatedAt',
}
